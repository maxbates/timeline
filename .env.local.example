# Anthropic Claude API Key
# No longer needed here - set your API key in the app's Settings UI (stored in browser localStorage)
# ANTHROPIC_API_KEY=sk-ant-api03-xxxxx

# Supabase Database URL
# Get this from your Supabase project settings -> Database -> Connection string (URI)
DATABASE_URL=postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres

# Supabase Direct URL (for Prisma migrations)
# Same as DATABASE_URL but may use port 5432 directly
DIRECT_URL=postgresql://postgres:[YOUR-PASSWORD]@db.[YOUR-PROJECT-REF].supabase.co:5432/postgres

# Map Tiles (optional - defaults to Stadia Maps)
# Get a free key at: https://stadiamaps.com/
NEXT_PUBLIC_STADIA_MAPS_API_KEY=your-stadia-maps-key

# Base URL for the app (used for generating share links)
NEXT_PUBLIC_BASE_URL=http://localhost:3000
