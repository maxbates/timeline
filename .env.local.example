# Anthropic Claude API Key
# No longer needed here - set your API key in the app's Settings UI (stored in browser localStorage)
# ANTHROPIC_API_KEY=sk-ant-api03-xxxxx

# Database URL (Neon PostgreSQL - pooled connection)
# Get this from your Neon dashboard -> Connection Details -> Connection string
DATABASE_URL=postgresql://[user]:[password]@[endpoint].neon.tech/[dbname]?sslmode=require

# Direct URL (for Prisma migrations - Neon direct connection)
# Use the direct (non-pooled) connection string from Neon
DIRECT_URL=postgresql://[user]:[password]@[endpoint].neon.tech/[dbname]?sslmode=require

# Map Tiles (optional - defaults to Stadia Maps)
# Get a free key at: https://stadiamaps.com/
NEXT_PUBLIC_STADIA_MAPS_API_KEY=your-stadia-maps-key

# Base URL for the app (used for generating share links)
# Update this to your Vercel URL after deploying
NEXT_PUBLIC_BASE_URL=http://localhost:3000
